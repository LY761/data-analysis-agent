# 当前剩余差距清单

## P0 已处理

- 非核心 RAG、联网搜索、爬虫、竞品、选品、旧 BI 已移除。
- 认证全路径绕过、脱敏失效、未脱敏缓存、SQLite 超时、表权限未接入已修复。
- Schema 改为高置信快路径与混合召回，输出结构化关系和证据。

## P1 待处理

1. API 响应和错误契约仍未全量统一。
2. LLM 客户端仍散落在 SQL、澄清、根因分析和报告模块。
3. 租户 ID 仍有部分接口由请求参数直接传入。
4. 数据源切换仍是进程级全局状态，不适合多租户并发。
5. 限流、熔断和热点缓存仍以内存或本机 SQLite 为主。
6. 认证用户、会话和 API Key 仍是 Demo 实现。
7. 核心 API 尚未全部声明 `response_model`。
8. 审计日志尚未形成统一 ToolCallRecord。

## P2 待处理

1. 工作流缺少分布式队列、死信和审批中心。
2. Schema 检索缺少离线标注集和召回阈值自动校准。
3. 前端尚未展示 Agent 决策、工具调用、兜底和审计证据。
4. 企业渠道只保留接口边界，尚未实现飞书、钉钉、微信适配器。
5. 生产部署、备份、监控告警和容量测试尚未完成。

整改顺序以 `MASTER_IMPLEMENTATION_PLAN.md` 为准。