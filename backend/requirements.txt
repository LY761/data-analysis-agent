# Data Analysis Agent - Backend Dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.33.0
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
chromadb>=0.5.0
sentence-transformers>=3.0
sqlglot>=25.0
pandas>=2.2.0
openai>=1.55
python-dotenv>=1.0.0
websockets>=14.0
pydantic>=2.10.0

# Optional: Production Infrastructure
redis>=5.0.0           # Redis cache for hot query caching (50ms hit)
langfuse>=2.0.0        # Langfuse tracing for full-chain observability
pymysql>=1.1.0         # MySQL support (switch via DB_TYPE=mysql)
pymilvus>=2.4.0        # Milvus vector store (switch via VECTOR_STORE=milvus)
